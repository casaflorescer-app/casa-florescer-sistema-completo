# Copie este arquivo para .env.local (nunca commite o .env.local).
# No Vercel: Settings → Environment Variables (Production, Preview e Development).

NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=cole_aqui_a_chave_anon_public

# NÃO coloque SUPABASE_SERVICE_ROLE_KEY no frontend / Vercel.
# Essa chave bypassa o RLS e só pode viver em Edge Functions ou no servidor.
